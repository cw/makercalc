<?xml version="1.0" encoding="utf-8"?>
<menu
  xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/cnc"
      android:onClick="onCncItemClick"
    	android:showAsAction="ifRoom|withText"
      android:title="@string/cnc" />
    <item android:id="@+id/math"
      android:onClick="onMathItemClick"
      android:showAsAction="ifRoom|withText"
      android:title="@string/math" />
    <item android:id="@+id/viz"
      android:onClick="onVizItemClick"
      android:showAsAction="ifRoom|withText"
      android:title="@string/viz" />
    <item android:id="@+id/settings"
      android:title="@string/settings" />
</menu>
